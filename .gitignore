# 手で取っていた旧バックアップ。
# これ以降は git が変更を記録するので、新しく作る必要はない。
# ファイル自体は消していないので、慣れてきたら手元で削除してよい。
demo.html.*

# OS や編集ソフトが勝手に作るもの
Thumbs.db
desktop.ini
.DS_Store
*.swp
node_modules/

# 個人的な内容を含む覚え書き。
# GitHub Pages を無料で使うとリポジトリは公開されるため、既定では記録に入れない。
# 中身: 英語のスコア、移住・ビザの計画、収入の話、氏名、経歴。
# 公開してよいと判断したら、この3行を消せば記録に入る。
働き方ロードマップ.md
europe-it-roadmap.md
旅情-SNS設計.md
