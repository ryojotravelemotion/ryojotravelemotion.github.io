# 旅情を公開する手順

上から順にやれば公開できます。所要は30分ほど。

---

## 1. GitHubのアカウントを作る

<https://github.com/signup> で作ります。メールアドレスとパスワードだけで無料です。

ユーザー名がそのまま公開アドレスの一部になります。あとから変えられますが、
リンクが切れるので最初に決めておくほうが楽です。

例: ユーザー名を `tsubu` にすると、公開先は `https://tsubu.github.io/ryojo/` になります。

---

## 2. 置き場所（リポジトリ）を作る

1. GitHubの右上「＋」→ **New repository**
2. **Repository name** に `ryojo` と入れる
3. **Public** を選ぶ（Privateだと無料では公開できません）
4. 下の「Add a README file」などは**すべてチェックを外す**（こちらに既にあるため）
5. **Create repository**

作られた画面に出てくるアドレス（`https://github.com/ユーザー名/ryojo.git`）を控えます。

> **公開されるもの・されないもの**
> 個人的な内容を含む3つの覚え書きは、記録から外してあります。
> ・働き方ロードマップ.md ・europe-it-roadmap.md ・旅情-SNS設計.md
> パソコンの中には残りますが、GitHubには上がりません。
> 公開してよいと判断したら `.gitignore` から該当の行を消してください。

---

## 3. パソコンからGitHubへ送る

このフォルダで、次の2行を順に実行します。
（`ユーザー名` は自分のものに置き換えてください）

```
git remote add origin https://github.com/ユーザー名/ryojo.git
git push -u origin main
```

途中でGitHubのログインを求められます。ブラウザが開くので、そこで許可してください。

1,652ファイル・約13MBあるので、送信に数分かかります。

---

## 4. 公開を有効にする

1. GitHubのリポジトリの画面で **Settings** → 左メニューの **Pages**
2. **Source** を「Deploy from a branch」にする
3. **Branch** を `main`、フォルダは `/ (root)` にして **Save**

1〜2分待つと、同じ画面に公開アドレスが出ます。

```
https://ユーザー名.github.io/ryojo/
```

開いて、冒頭のデモが流れれば成功です。

---

## 5. 【必須】Mapboxのトークンに制限をかける

**これをやらないと、他の人にトークンを使われて旅情が止まります。**
請求は来ませんが（カード未登録のため）、無料枠を使い切ると地図が出なくなります。

1. <https://account.mapbox.com/access-tokens/> を開く
2. いま使っているトークン（`pk.` で始まるもの）を開く
3. **URL restrictions** に、次の2つを追加する

```
https://ユーザー名.github.io/*
http://localhost:5501/*
```

**2つめを忘れると、手元のLive Serverで地図が出なくなります。**

---

## 6. SNS用のアドレスを埋める

公開アドレスが決まったので、`index.html` の上のほうにある2か所を直します。

```html
<meta property="og:image" content="ogp.jpg">
<meta property="og:url"   content="">
```

↓

```html
<meta property="og:image" content="https://ユーザー名.github.io/ryojo/ogp.jpg">
<meta property="og:url"   content="https://ユーザー名.github.io/ryojo/">
```

`twitter:image` も同じように直します。直したら、また送ります。

```
git add -A
git commit -m "公開アドレスを設定"
git push
```

---

## これ以降、直したものを反映するには

```
git add -A
git commit -m "何を直したか"
git push
```

この3行だけです。1〜2分で公開先に反映されます。

---

## 困ったときの確認

**地図が出ない**
Mapboxのトークン制限のアドレスが合っているか確認してください。
`https://` と `http://`、末尾の `/*` の有無で変わります。

**ページが真っ白**
Settings → Pages の Branch が `main` / `/ (root)` になっているか。

**変更が反映されない**
`git push` を忘れていないか。GitHubのリポジトリ画面で、
最新の記録が上がっているか確認できます。
